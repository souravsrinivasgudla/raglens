apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig
 
metadata:
  name: raglens-cluster
  region: us-east-1
  version: "1.35"
 
managedNodeGroups:
  - name: raglens-ng
    instanceType: t3.medium
    minSize: 3
    maxSize: 5
    desiredCapacity: 3
    volumeSize: 20
    iam:
      withAddonPolicies:
        albIngress: true
        cloudWatch: true
 
addons:
  - name: vpc-cni
  - name: coredns
  - name: kube-proxy
 
iam:
  withOIDC: true


